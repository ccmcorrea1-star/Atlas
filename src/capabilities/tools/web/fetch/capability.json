{
  "id": "web.fetch",
  "type": "tool",
  "summary": "obter, abrir ou ler o conteudo de uma pagina, link ou URL via HTTP/HTTPS",
  "description": "busca uma pagina com redirects limitados, timeout e limite de tamanho; extrai texto util de HTML e preserva texto puro e JSON; rejeita esquemas nao suportados; nunca executa JavaScript",
  "schema": {
    "type": "object",
    "properties": {
      "url": {
        "type": "string",
        "description": "URL HTTP ou HTTPS a buscar"
      }
    },
    "required": ["url"],
    "additionalProperties": false
  },
  "parent": "web",
  "aliases": ["curl"],
  "implementation": {
    "kind": "executable",
    "entrypoint": "runtime"
  }
}
