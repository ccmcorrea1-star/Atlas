{
  "id": "web.search",
  "type": "tool",
  "summary": "pesquisar na web por tutoriais, documentacao ou respostas",
  "description": "consultar a internet por documentacao, tutoriais e resultados atualizados; sem provider configurado retorna unavailable",
  "schema": {
    "type": "object",
    "properties": {
      "query": {
        "type": "string",
        "description": "consulta de busca"
      },
      "limit": {
        "type": "integer",
        "minimum": 1,
        "description": "número máximo de resultados (padrão 5, máximo 20)"
      }
    },
    "required": ["query"],
    "additionalProperties": false
  },
  "parent": "web",
  "aliases": [],
  "implementation": {
    "kind": "executable",
    "entrypoint": "runtime"
  }
}
