{
  "id": "process.exec",
  "type": "tool",
  "summary": "executa um processo diretamente sem shell",
  "description": "executar um programa local diretamente, sem shell",
  "schema": {
    "type": "object",
    "properties": {
      "program": {
        "type": "string",
        "description": "programa ou caminho do executavel"
      },
      "args": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "argumentos literais do programa"
      },
      "cwd": {
        "type": "string",
        "description": "diretorio de trabalho opcional"
      },
      "timeout_ms": {
        "type": "integer",
        "minimum": 0,
        "description": "limite de tempo em milissegundos"
      }
    },
    "required": ["program"],
    "additionalProperties": false
  },
  "parent": "process",
  "aliases": [],
  "implementation": {
    "kind": "executable",
    "entrypoint": "implementation"
  }
}
