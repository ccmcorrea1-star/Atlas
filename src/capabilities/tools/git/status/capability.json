{
  "id": "git.status",
  "type": "tool",
  "summary": "retorna estado estruturado do working tree e staging",
  "description": "executa git status na maquina local e devolve branch, arquivos do staging, nao staged e nao rastreados, sem alterar o repositorio",
  "schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "diretorio do repositorio"
      }
    },
    "required": ["path"],
    "additionalProperties": false
  },
  "parent": "git",
  "aliases": ["estado do repositorio", "git status", "arquivos modificados"],
  "implementation": {
    "kind": "executable",
    "entrypoint": "runtime"
  }
}
