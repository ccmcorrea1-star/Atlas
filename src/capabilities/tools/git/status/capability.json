{
  "id": "git.status",
  "type": "tool",
  "summary": "retorna estado estruturado do working tree e staging",
  "description": "executa git status e devolve a branch, arquivos modificados, staged e nao rastreados, sem alterar o repositorio",
  "schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "diretorio do repositorio"
      }
    },
    "required": ["path"],
    "additionalProperties": false
  },
  "parent": "git",
  "aliases": [],
  "implementation": {
    "kind": "executable",
    "entrypoint": "runtime"
  }
}
