{
  "id": "git.diff",
  "type": "tool",
  "summary": "mostrar o diff do repositorio, do working tree ou do staged",
  "description": "executa git diff para mostrar as mudancas locais sem alterar o repositorio; trunca saida longa",
  "schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "diretorio do repositorio"
      },
      "staged": {
        "type": "boolean",
        "description": "true usa --staged; ausente ou false usa working tree"
      }
    },
    "required": ["path"],
    "additionalProperties": false
  },
  "parent": "git",
  "aliases": [],
  "implementation": {
    "kind": "executable",
    "entrypoint": "runtime"
  }
}
