{
  "id": "lsp.diagnostics",
  "type": "tool",
  "summary": "ver erros e avisos de codigo via language server",
  "description": "analisa um arquivo com o language server e retorna diagnosticos, erros e avisos sem executar build ou lint como substituto",
  "schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "caminho do arquivo a analisar"
      }
    },
    "required": ["path"],
    "additionalProperties": false
  },
  "parent": "lsp",
  "aliases": ["typecheck"],
  "implementation": {
    "kind": "executable",
    "entrypoint": "runtime"
  }
}
