{
  "id": "filesystem.search",
  "type": "tool",
  "summary": "buscar texto ou codigo em arquivos de um projeto",
  "description": "procurar funcoes, trechos ou texto no conteudo dos arquivos do projeto",
  "schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "caminho explicito do diretorio raiz da busca"
      },
      "query": {
        "type": "string",
        "description": "substring literal buscada, sensivel a maiusculas"
      },
      "max_results": {
        "type": "integer",
        "minimum": 1,
        "description": "limite de resultados; padrao 200"
      }
    },
    "required": ["path", "query"],
    "additionalProperties": false
  },
  "parent": "filesystem",
  "aliases": ["grep", "ripgrep"],
  "implementation": {
    "kind": "executable",
    "entrypoint": "runtime"
  }
}
