{
  "id": "filesystem.list",
  "type": "tool",
  "summary": "listar cada arquivo e o conteudo de um diretorio",
  "description": "listar as entradas de um diretorio, em ordem alfabetica",
  "schema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "caminho explicito do diretorio"
      }
    },
    "required": ["path"],
    "additionalProperties": false
  },
  "parent": "filesystem",
  "aliases": ["ls"],
  "implementation": {
    "kind": "executable",
    "entrypoint": "runtime"
  }
}
